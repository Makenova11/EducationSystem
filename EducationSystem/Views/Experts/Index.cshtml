@model IEnumerable<EducationSystem.Models.Expert>

@{
    ViewBag.Title = "Index";
}


<table class="table">
    <caption>Эксперты</caption>
    <tr>
        <th>
            Фамилия
        </th>
        <th>
            Имя
        </th>
        <th>
            Отчество
        </th>
        <th>
            Email
        </th>
        <th>
            Телефон
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.SecondName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.FirstName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Patronymic)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Email)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Phone)
    </td>

    @*<td>
        <p>
            <a class="btn btn-default" @Html.ActionLink("Редактировать", "Edit", new { id = item.ID_работника }) |
                    <a class="btn btn-default" @Html.ActionLink("Удалить", "Delete", new { id = item.ID_работника })
        </p>
    </td>*@
</tr>
    }
</table>
